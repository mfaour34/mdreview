{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "case \"$CLAUDE_FILE_PATH\" in *.py) ruff format --quiet \"$CLAUDE_FILE_PATH\" && ruff check --fix --quiet \"$CLAUDE_FILE_PATH\" ;; esac",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
