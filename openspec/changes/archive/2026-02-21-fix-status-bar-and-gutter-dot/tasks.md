## 1. Style footer bar key bindings

- [x] 1.1 Update `FooterBar.NORMAL` to use Rich markup so key bindings (e.g., `c`, `f`, `←→`) render in a distinct color from their descriptions
- [x] 1.2 Update `FooterBar.SELECTING` to use the same Rich markup styling for its key bindings

## 2. Update inline-comments spec

- [x] 2.1 Replace `●` gutter marker references in `openspec/specs/inline-comments/spec.md` with left-border indicator language
